import { Link } from 'react-router-dom';
const Navbar = () => {
  return (
    <nav className='navbar navbar-dark bg-dark'>
      <a href='/'>postTracker</a>
    </nav>
  );
};

export default Navbar;
